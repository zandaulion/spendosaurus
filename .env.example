# Spendosaurus Environment Configuration
PORT=3090
DATA_DIR=/data
PUBLIC_BASE_URL=https://spendosaurus.example.com

# Shared secret for /api/admin/*, injected by the tailnet-only listener.
# Generate with: openssl rand -hex 32
ADMIN_TOKEN=
